meta {
  name: List Wallets
  type: http
  seq: 3
}

get {
  url: {{base_url}}/v1/wallets
  auth: none
}

headers {
  authorization: Bearer {{token}}
}

docs {
  Cursor-paginated. Response shape is {data: [...], next_cursor}; pass ?before=<cursor> for
  the next page.
}
