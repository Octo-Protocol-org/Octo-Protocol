[package]
name = "octo-evm-rpc"
description = "Typed JSON-RPC 2.0 client for EVM chains, with per-chain resilience honouring the submit-asymmetry rule."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
octo-resilience.workspace = true
reqwest.workspace = true
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
ethnum.workspace = true
futures-util.workspace = true

[dev-dependencies]
tokio.workspace = true
axum.workspace = true
proptest.workspace = true
# Pinned: newer 0.6.x requires edition2024 (Rust >= 1.85), but rust-toolchain.toml pins 1.84.1,
# so an unpinned "0.6" resolves to a version this project's own toolchain cannot build.
wiremock = "=0.6.2"
